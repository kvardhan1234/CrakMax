# Backend

This project currently does not have a live backend.

The exam application is implemented as a static frontend. If you want API support later, this folder is ready for:
- authentication
- question delivery APIs
- score submission APIs
- result storage
