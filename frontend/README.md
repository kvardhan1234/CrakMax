# Frontend

This folder contains the CrakMax exam UI.

Current app assets and runtime files remain at the repository root for compatibility:
- `index.html`
- `styles.css`
- `script.js`
- `data/`
- `assets/`

If you want, this can be moved fully into `frontend/` in a follow-up pass.
